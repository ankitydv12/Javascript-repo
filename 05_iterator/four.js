// ********************************for..in*********************************************
        //use for object and arrays

const myObject = {
    js: 'javascript',
    cpp: 'C++',
    rb: "ruby",
    swift: "swift by apple"
}

for(const key in myObject)
    console.log(`${key} ${myObject[key]}`);

const arr= [1,2,56,78 ]
for(const a in arr)
    console.log(arr[a])


